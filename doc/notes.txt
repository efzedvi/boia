Unit Tests for Tail:

Basic01: set_files, tail with timeout, write to one, tail, check

Delete01: set_files, tail with timeout, write, delete, tail, check

rename01: set_files, tail with timeout, write, rename, tail check
rename02: set_files, tail with timeout, write, rename, re-create, write, tail check
